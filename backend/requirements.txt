# PDF + DOCX parsing
pypdf==5.1.0
python-docx==1.1.2

# Chunking — just the splitter package, not all of LangChain
langchain-text-splitters==0.3.2

# Week 2 — Embeddings (CPU PyTorch, MiniLM model)
sentence-transformers==3.3.1

# Week 2 — Vector database (local persistent)
chromadb==0.5.23

# Week 4 — LLM client (Google Gemini, free tier)
google-generativeai==0.8.3

# Week 4 — Load API keys from .env file
python-dotenv==1.0.1

# Pinned for reproducibility on Python 3.11
