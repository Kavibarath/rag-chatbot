# PDF + DOCX parsing
pypdf==5.1.0
python-docx==1.1.2

# Chunking — just the splitter package, not all of LangChain
langchain-text-splitters==0.3.2

# Used by ingest.py for stable IDs
# (uuid is stdlib, no install needed)

# Pinned for reproducibility on Python 3.11
